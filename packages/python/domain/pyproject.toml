[project]
name = "image-model-lab-domain"
version = "0.1.0"
description = "Framework-independent domain package for the image model lab."
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.0,<0.12.0"]
build-backend = "uv_build"
