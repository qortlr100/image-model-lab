; Alembic configuration for the image model lab schema.
;
; It lives inside the package directory so it is packaged into the wheel:
; whatever installs this package carries the migrations that match the mapping
; it reads. No service does yet -- each service image installs only its own
; dependency closure and none of them depends on the persistence package -- so
; the way to run a migration today is from the repository workspace:
;
;     just migrate upgrade head
;     just migrate downgrade base
;
; There is deliberately no `sqlalchemy.url` here. The URL carries a password
; and differs per deployment, so `env.py` reads it from the environment
; variable every service reads.

[alembic]
script_location = %(here)s/migrations
prepend_sys_path = .
path_separator = os
timezone = UTC

; Revision ids are sequential and zero-padded rather than random hex, so
; `versions/` reads in the order the revisions were written.
file_template = %%(rev)s_%%(slug)s

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
