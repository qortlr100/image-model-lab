#!/usr/bin/env bash
# Block a commit that would add artifacts, oversized files or credentials.
# Install with: just install-hooks
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
exec python3 "${repo_root}/tools/repo_policy.py" --mode staged
